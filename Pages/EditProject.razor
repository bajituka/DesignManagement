@page "/projects/{Id}"
@inject ProjectDataAccessLayer.IProjectService service
@inject NavigationManager NavigationManager

<h3>@ProjectInContext.Name</h3>

<ProjectDetails ProjectObject=ProjectInContext></ProjectDetails>

@code {
    [Parameter]
    public string Id { get; set; }

    Project ProjectInContext;

    protected override async void OnParametersSet()
    {
        ProjectInContext = await service.Get(Int32.Parse(Id));
    }
}

